"""qto — 베이스라인 라운드 산출물 패키지 (수량/중량 산출).

베이스라인-1: 단위중량을 `단면적 × 밀도(7,850 kg/m³)` 한 식으로 통일한
`unit_weight_calc` 모듈을 둔다. 보류 중인 `poc_v2/length/unit_weight.py`
(yaml 룩업) 와는 별개 — 룩업 분기 없이 계산만으로 단위중량을 구한다.
"""
